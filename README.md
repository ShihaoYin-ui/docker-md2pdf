# md2pdf-docker

Convert Markdown to beautifully styled PDF using Pandoc + WeasyPrint in Docker — no local environment setup required.

📄 在 Docker 中使用 Pandoc + WeasyPrint 将 Markdown 转换为样式精美的 PDF，无需配置本地环境。
